name: store_it_app
description: A new Flutter project.


version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.0+2
  cupertino_icons: ^0.1.2
  camera:
  path_provider:
  path:
  image_picker: "^0.4.5"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_cupertino_date_picker: ^1.0.12
  intl: ^0.15.8
  flutter_spinkit: ^4.0.0

flutter:
  assets:
    - assets/
  fonts:
    - family: Rock_Salt
      fonts:
        - asset: RockSalt-Regular.ttf
          weight: 400
    - family: Roboto
      fonts:
        - asset: Roboto-Regular.ttf
          weight: 400
  uses-material-design: true